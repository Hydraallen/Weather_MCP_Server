services:
  weather-mcp:
    build: .
    container_name: weather-mcp-server
    ports:
      - "8000:8000"
    environment:
      - MCP_TRANSPORT=sse
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
